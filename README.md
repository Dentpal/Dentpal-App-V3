samples, guidance on mobile development, and a full API reference.

# dentpal

This is a Flutter project scaffolded using `flutter create .`.

## Getting Started

To run the app on your device or emulator:

1. Make sure you have Flutter installed. See https://docs.flutter.dev/get-started/install
2. In the project directory, run:

	flutter run

This will launch the app on the connected device or emulator.

## Project Structure
- `lib/main.dart`: Main entry point of the app
- `pubspec.yaml`: Project configuration and dependencies

For more details, see the official Flutter documentation: https://docs.flutter.dev/
